"""Data seeding — intentionally empty.

The default admin/admin seed was removed in favour of the first-run setup
wizard (POST /auth/setup/init). New installations create their admin account
interactively via the UI.
"""
