# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.20

# Data validation and settings
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Authentication and security
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt==4.0.1
cryptography>=43.0.0

# Scheduling (minimal)
apscheduler>=3.10.0

# Task queue (Celery + RabbitMQ + Redis)
celery[redis]>=5.3.0
celery-redbeat>=2.0.0
redis>=5.0.0
flower>=2.0.0

# Monitoring and metrics
prometheus-client>=0.19.0

# Structured logging
python-json-logger>=2.0.7

# Rate limiting
slowapi>=0.1.9

# YAML configuration
pyyaml>=6.0

# Faker is used in production by the generate-data endpoint (not just for tests)
Faker>=22.0.0
watchdog>=4.0.0

# HTTP client (used by existing endpoints)
httpx>=0.25.0

# Pydantic AI for multi-agent orchestration
pydantic-ai>=1.0.0

# Database connectors (DataPulse)
metronome-pulse-postgres>=0.1.0

# Migrations (Alembic + async drivers for SQLAlchemy)
alembic>=1.13.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Observability (tracing, spans, evals) - Pydantic Logfire
# 4.31+ pulls OpenTelemetry versions compatible with logfire imports (ReadableLogRecord chain)
logfire[fastapi,httpx]>=4.31.0
